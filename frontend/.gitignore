node_modules/
dist/
dist-electron/
release/
.vite/

# #62/#80(Q-17): prepare-python-runtime.mjsが生成する同梱用Pythonランタイム。
# サイズが大きく(数百MB)、Windows専用バイナリのためリポジトリに含めない。
python-runtime/
ffmpeg/
# 上記の組み立て作業用一時ディレクトリ。プロセスが強制終了された場合、
# finallyでの削除が走らず残存しうる(#62 Gate2レビュー指摘)。
.python-runtime-tmp/
